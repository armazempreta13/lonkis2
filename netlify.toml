[build]
command = "npm ci --prefer-offline --no-audit && npm run build"
functions = "functions"
publish = "dist"

[build.environment]
NODE_VERSION = "22"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
